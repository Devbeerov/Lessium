<UserControl x:Class="Lessium.ContentControls.ContentPageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lessium.ContentControls"
             xmlns:models="clr-namespace:Lessium.Models"
             xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >

    <ListBox x:Name="itemsControl"
             VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
             ScrollViewer.CanContentScroll="False" 
             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
             ScrollViewer.VerticalScrollBarVisibility="Disabled"
             MaxWidth="{x:Static models:ContentPageModel.PageWidth}"
             Focusable="{Binding ElementName=itemsControl, Path=HasItems}"
             ItemsSource="{Binding Items}"
             Keyboard.KeyDown="itemsControl_KeyDown" 
             PreviewMouseWheel="itemsControl_PreviewMouseWheel">

        <ListBox.Resources>

            <Style x:Key="DeselectListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
                <EventSetter Event="LostFocus" Handler="ListBoxItem_LostFocus" />
            </Style>

            <Style x:Key="FixedWidthStyle" TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource DeselectListBoxItemStyle}">
                <!--Value="{x:Static models:ContentPageModel.PageWidth}"-->
                <Setter Property="MaxWidth" >
                    <Setter.Value>
                        <MultiBinding Converter="{StaticResource ArithmeticConverter}" ConverterParameter="-" >
                            <Binding Source="{x:Static models:ContentPageModel.PageWidth}" />
                            <Binding RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBox}}" Path="BorderThickness.Left" />
                            <Binding RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBox}}" Path="BorderThickness.Right" />
                        </MultiBinding>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="UnselectableItemsStyle" TargetType="ListBoxItem" BasedOn="{StaticResource FixedWidthStyle}">
                <Style.Resources>
                    <!-- SelectedItem with focus -->
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                       Color="Transparent" />
                    <!-- SelectedItem without focus -->
                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"
                       Color="Transparent" />
                    <!-- SelectedItem text foreground -->
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"
                       Color="Transparent" />
                </Style.Resources>
                <!--<Setter Property="FocusVisualStyle" Value="{x:Null}" />-->
            </Style>

        </ListBox.Resources>

        <ListBox.Style>
            <Style TargetType="{x:Type ListBox}">
                <Setter Property="ItemContainerStyle" Value="{StaticResource UnselectableItemsStyle}" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:ContentPageControl}, Path=IsReadOnly}" Value="False">
                        <DataTrigger.Setters>
                            <Setter Property="ItemContainerStyle" Value="{StaticResource FixedWidthStyle}" />
                        </DataTrigger.Setters>
                        
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ListBox.Style>

    </ListBox>
    
</UserControl>
