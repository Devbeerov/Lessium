<UserControl x:Class="Lessium.ContentControls.ContentPageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lessium.ContentControls"
             xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase" xmlns:materialcontrols="clr-namespace:Lessium.ContentControls.MaterialControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="control" >

    <ListBox x:Name="itemsControl"
             VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
             Focusable="{Binding ElementName=itemsControl, Path=HasItems}"
             ItemsSource="{Binding Items}" >

        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel Orientation="Vertical" />
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        
    </ListBox>
    
</UserControl>
