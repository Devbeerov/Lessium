<UserControl x:Class="Lessium.ContentControls.TestControls.SimpleTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lessium.ContentControls.TestControls"
             xmlns:properties="clr-namespace:Lessium.Properties"
             xmlns:materials="clr-namespace:Lessium.ContentControls.MaterialControls"
             xmlns:tests="clr-namespace:Lessium.ContentControls.TestControls"
             xmlns:converters="clr-namespace:Lessium.Converters"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
             xmlns:utility="clr-namespace:Lessium.Utility"
             xmlns:cc="clr-namespace:Lessium.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             x:Name="control" >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Themes/MainViewDictionary.xaml" />
                
                <!-- For SimpleTest only -->
                <ResourceDictionary>
                    <Style x:Key="checkBoxStyle" TargetType="{x:Type CheckBox}">
                        <Style.Setters>
                            <Setter Property="Width" Value="16" />
                            <Setter Property="Height" Value="16" />
                            <Setter Property="HorizontalAlignment" Value="Left" />
                            <Setter Property="VerticalAlignment" Value="Top" />
                            <Setter Property="IsChecked" Value="True" />
                        </Style.Setters>
                    </Style>
                </ResourceDictionary>
                
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border x:Name="border" MinWidth="16" MinHeight="16" SizeChanged="Border_SizeChanged">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left" >

            <StackPanel x:Name="contentPanel" Orientation="Vertical">

                <materials:TextControl x:Name="testQuestion"
                                       ToolTip="{x:Static properties:Resources.TextControl_Tooltip}"
                                       Text="{Binding Question, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=local:SimpleTest}}"
                                       ShowRemoveButton="False" 
                                       SizeChanged="Question_SizeChanged">

                </materials:TextControl>

                <ItemsControl x:Name="AnswersItemControl" ItemsSource="{Binding Answers}" SizeChanged="AnswersItemControl_SizeChanged" >

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" x:Name="dataPanel">

                                <!-- ReadOnly version -->
                                <cc:RadioCheckBox x:Name="AnswerSelectionBox"
                                                  Style="{StaticResource }"
                                                  IsEnabled="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                  Visibility="{Binding RelativeSource={RelativeSource Mode=Self}, Path=IsEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                <!-- Editing version -->
                                <CheckBox x:Name="ToggleAnswerTrue" Width="16" Height="16"
                                          HorizontalAlignment="Left" VerticalAlignment="Top"
                                          IsChecked="False"
                                          Checked="ToggleAnswerTrue_Checked"
                                          Unchecked="ToggleAnswerTrue_Unchecked"
                                          IsEnabled="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource InverseBooleanConverter}}" 
                                          Visibility="{Binding RelativeSource={RelativeSource Mode=Self}, Path=IsEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" />
  
                                <!-- TextControl. It's have it's own DataContext, so we bind to dataPanel.DataContext -->
                                <materials:TextControl x:Name="TextContainer"
                                                       Text="{Binding ElementName=dataPanel, Path=DataContext.Text, Mode=TwoWay}"
                                                       Loaded="TextContainer_Loaded"
                                                       Unloaded="TextContainer_Unloaded"
                                                       RemoveControl="RemoveAnswer_Click" >

                                    <materials:TextControl.Tag>
                                        <MultiBinding Converter="{StaticResource UIElementsDistanceConverter}" ConverterParameter="X">
                                            <Binding ElementName="TextContainer" />
                                            <Binding RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                                        </MultiBinding>
                                    </materials:TextControl.Tag>

                                    <materials:TextControl.MaxWidth>
                                        <MultiBinding Converter="{StaticResource ArithmeticConverter}" ConverterParameter="-">
                                            <Binding RelativeSource="{RelativeSource AncestorType=ItemsControl}" Path="MaxWidth" />
                                            <Binding Path="Tag" />
                                        </MultiBinding>
                                    </materials:TextControl.MaxWidth>

                                </materials:TextControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>

                <!-- Using code-behind instead of bindings, because there will be direct value changes and it will break bindings. -->
                <Button x:Name="addAnswerButton" Click="AddAnswer_Click" Width="16" Height="16"
                        HorizontalAlignment="Left" > <!-- Have additional properties changes in code-behind (Border_SizeChanged) -->

                    <Image x:Name="addAnswerIcon" Source="{StaticResource ResourceKey=AddContentIcon}" />
                </Button>

            </StackPanel>

            <Button x:Name="removeButton" Click="RemoveButton_Click" Width="16" Height="16"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    IsEnabled="{Binding IsReadOnly}" 
                    Visibility="{Binding RelativeSource={RelativeSource Mode=Self}, Path=IsEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" >

                <Image x:Name="removeContentIcon" Source="{StaticResource ResourceKey=RemoveContentIcon}" />
            </Button>

        </StackPanel>
    </Border>
</UserControl>